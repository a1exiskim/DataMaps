# DataMaps

DataMaps is a platform tool that discovers, organizes, and eventually visualizes data infrastructure, given a data source. Similar to Google Maps, but instead of navigating places, you can navigate data systems! 
